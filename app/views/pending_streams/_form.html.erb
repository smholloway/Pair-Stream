<%= form_for(@pending_stream) do |f| %>
  <% if @pending_stream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pending_stream.errors.count, "error") %> prohibited this pending_stream from being saved:</h2>

      <ul>
      <% @pending_stream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requestor %><br />
    <%= f.text_field :requestor %>
  </div>
  <div class="field">
    <%= f.label :requestee %><br />
    <%= f.text_field :requestee %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
